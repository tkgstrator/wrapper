{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.c|*.cpp|*.cc|*.cxx|*.h|*.hpp|*.hxx) command -v clang-format >/dev/null 2>&1 && clang-format -i \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "clang-format"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/auto-commit.sh"
          }
        ]
      }
    ]
  }
}
